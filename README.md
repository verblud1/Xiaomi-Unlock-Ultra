# Xiaomi-Unlock-Ultra



javascript :(function(){var token=document.cookie.match(/popRunToken=([^;]+)/);if(token){prompt("Copy the token:", token[1]);}else{alert("Token not found");}})()

в независимости от того, какой результат был получен в первые несколько минут после "00:00:00" по Пекину делаем следующие:
 # 
#1
настройки » расширенные настройки » для разработчиков » статус Mi Unlock » связать аккаунт с устройством.

 # 
#2
если не помог #1, иначе сразу к п.5:
настройки » mi аккаунт » выйти из аккаунта » далее перезагрузить устройство;
после перезагрузки: настройки » расширенные настройки » для разработчиков » статус Mi Unlock » связать аккаунт с устройством.


не обращая внимания на то, какой результат был получен, переходим и пытаемся разблокировать (на Ваш выбор) в:
Miunlock. Иногда бывает, что в разработчиках написало "Привязан", а при разблокироке просит снова привязать, в этом случае:
перезагрузите телефон;
запустите снова Miunlock;
если не помогло, то попробуйте сменить версию или используйте другие программы для разблокировки (приведены ниже).

Прикрепленный файлMiUnlockTool.zip ( 6.81 МБ )
(можно обойти получение смс, сделав вход через emai), запустив предварительно Прикрепленный файлInstMod.zip ( 1.29 КБ )
(установит необходимые библиотеки, если нет). Лучше python-скрипт запускать из-под командной строки с правами администратора (будет возможность контролировать весь процесс);
XUT (не требует смс, верификация через email).
